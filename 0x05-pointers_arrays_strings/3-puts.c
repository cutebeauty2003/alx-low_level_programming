#include "main.h"

/**
 * _puts - write a script that prints a string
 * followed by a new line, to stdout
 * @str: pointer to the string to print
 * Return: void
 */

void _puts(char *str)
{
	int = 0;

	while (str[i])
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
